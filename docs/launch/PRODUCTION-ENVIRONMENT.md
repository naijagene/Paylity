# Production Environment Matrix

See PAY-033 report for full variable list. Never commit secrets. `APP_ENV=production`, `APP_DEBUG=false`, dedicated Paystack live keys, VTPass production host, strong `OPERATOR_ACCESS_KEY`.
