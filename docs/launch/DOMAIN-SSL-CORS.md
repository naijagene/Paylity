# Domain, SSL, and CORS

Configure `paylity.ng`, `api.paylity.ng`, `ops.paylity.ng`. Set `FRONTEND_URL`, `APP_URL`, `CORS_ALLOWED_ORIGINS` to production origins only. Verify HTTPS on API and customer domains.
