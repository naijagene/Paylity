# VTPass Live Checklist

- `VTPASS_ENV=production`, live base URL, credentials configured
- `VTPASS_SANDBOX_TESTS=false`, auto-fulfill enabled intentionally
- Run `php artisan paylity:vtpass-mode` and `paylity:vtpass-check`
